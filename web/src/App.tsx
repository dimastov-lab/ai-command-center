// Minimal placeholder — the real Home screen ships in Task 4, built on top
// of the design tokens (src/theme/tokens.css) and API client (src/lib/api.ts)
// scaffolded here.
function App() {
  return (
    <div className="glass" style={{ margin: '2rem', padding: '1.5rem', color: 'var(--tx)' }}>
      <h1 style={{ color: 'var(--tx)' }}>AI Command Center</h1>
      <p style={{ color: 'var(--tx2)' }}>Home screen coming in a later task.</p>
    </div>
  )
}

export default App
